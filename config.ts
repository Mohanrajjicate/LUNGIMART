// Use a relative path, assuming the frontend and backend are served from the same domain.
// This is a common setup for Hostinger deployments.
// If your backend is on a different domain, replace with the full URL, e.g., 'https://api.yourdomain.com'
export const API_BASE_URL = '/api';
